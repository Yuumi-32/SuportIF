/// Resposta das ações do painel do professor. O texto vira o aviso que aparece
/// no topo da página, então já chega pronto para leitura.
export type TeacherActionResult = { ok: boolean; message: string };
